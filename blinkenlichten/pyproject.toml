[project]
name = "blinkenlichten"
version = "0.2.0"
description = "LED controller via WLED JSON API with GitHub webhook integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = []

